import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  badgeContainer: {
    backgroundColor: 'red',
    marginTop: 0,
    marginBottom: 7,
    borderRadius: 10,
    borderColor: 'red',
    overflow: 'hidden'
  },
  badgeCount: {
    color: '#FFFFFF',
    fontSize: 11
  }
})
