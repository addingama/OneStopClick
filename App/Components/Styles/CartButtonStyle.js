import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  badgeContainer: {
    backgroundColor: 'red',
    marginTop: 15,
    borderWidth: 1,
    borderRadius: 10,
    borderColor: 'red',
    overflow: 'hidden'
  },
  badgeCount: {
    color: '#FFFFFF',
    fontSize: 11
  }

})
